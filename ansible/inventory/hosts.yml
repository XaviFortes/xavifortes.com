# ansible/inventory/hosts.yml
#
# Ansible inventory for xavifortes.com infrastructure.
# Connection details (host IP, user, key) are intentionally omitted here.
# Ansible resolves the host via the SSH alias in ~/.ssh/config.
# Sensitive connection vars live in host_vars/vps-f24bf8b4/vault.sops.yml (encrypted).
all:
  children:
    gra8:
      hosts:
        vps-f24bf8b4:
