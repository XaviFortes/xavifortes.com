---
# kubernetes/apps/xavifortes-web/kustomization.yaml
#
# Kustomize root for the xavifortes-web application.
# ArgoCD's Application spec points at this directory.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
